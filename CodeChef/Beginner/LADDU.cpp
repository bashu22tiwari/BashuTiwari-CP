#include <bits/stdc++.h>
#include <cstdlib>
#include <algorithm>
#include <cstring>
#include <vector>
using namespace std;

int main()
{
    int tt;
    cin >> tt;
    while(tt--)
    {
        int n;
        cin >> n ;
        string origin;
        cin >> origin;
        int count=0 ;

    for(int i=0 ; i<n ; i++)
    {
        if(i==0)
        {
            string s;
            cin >> s;
            int rank;
            cin >> rank;

            if(rank)
        }
    }

        
        
    }

   

}



    