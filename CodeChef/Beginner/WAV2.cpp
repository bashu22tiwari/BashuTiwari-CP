#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    long long int n,q;
    cin >> n >> q ;
    
    long long int roots[n];
    for(long long int i=0 ; i<n ; i++){
        cin >> roots[i];
    }

    sort(roots,roots+n);

    for(long long int i=0 ; i<q ; i++){
        int given;
        cin >> given;
        
    }
}