#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int tt;
    cin >> tt;
    while(tt--){
        int G,p;
        cin >> G >> p;
        int arr[10];
        for(int i=0 ; i<10 ; i++){
            cin >> arr[i] ;
        }

        for(int i=9 ; i>=G ; i--){
            if()
        }
    }
}